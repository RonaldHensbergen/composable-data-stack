# Draft PR: binded-secrets — profile secret resolution and renderer fixes

This draft PR includes the same content as the earlier draft but placed in `.github/` so it isn't ignored by git.

See `build/PR_DRAFT.md` for full details (copy remains for convenience).
